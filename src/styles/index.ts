const bigButton = `text-green cursor-pointer border border-green rounded text-sm font-mono transition-all duration-300 ease-in-out hover:bg-heroHover focus:bg-heroHover active:bg-heroHover`

const section = `flex justify-center items-center min-h-screen px-6 md:px-12 w-screen border`

const sectionContent = `flex flex-col justify-between gap-6 font-sans border`

export { bigButton, section, sectionContent }
